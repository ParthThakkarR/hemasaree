# Test Coverage Audit Report — HemaSaree

_Generated: 2026-05-20_

---

## 1. Test Infrastructure

| Layer         | Framework         | Location                  |
|---------------|-------------------|---------------------------|
| Unit tests    | Vitest + jsdom    | `tests/unit/`             |
| E2E tests     | Playwright        | `tests/e2e/`              |
| Config        | `vitest.config.ts` | aliases: `@`, `@lib`, `@components`, `@contexts`, `@utils` |

Scripts: `npm test` (vitest), `npm run test:e2e` (playwright), `npm run test:all` (type-check + lint + test + e2e)

---

## 2. Total Files Audited

- **25 test files** (14 unit API/service/validators + 6 component + 1 errors + 3 misc)
- **35+ API route handlers** (`app/api/**`)
- **13 admin-only API routes** (`app/api/admin/**`)
- **~15 library modules** in `lib/` and `app/lib/`
- **~20+ page components** in `app/`

---

## 3. UNIT TEST COVERAGE — What IS Tested

### ✅ API Routes (6 test files)
| Test File | Route Under Test | What's Covered |
|-----------|----------------|---|---|
| `tests/unit/api/auth/signup.test.ts` | `POST /api/(auth)/signup` | 201 (user created), 409 (duplicate), 400 (validation fail), 500 (DB error), missing-address edge case |
| `tests/unit/api/auth/routeImportTest.test.ts` | `POST /api/(auth)/signup` import | Trivial: verifies route handler is a callable function |
| `tests/unit/api/auth/direct-import.test.ts` | `POST /api/(auth)/signup` import | Same — import-only check |
| `tests/unit/api/products.test.ts` | `GET /api/products` | Cache hit, cache miss + DB fetch, pagination validation, filter extraction, cache set after fetch, error handling |
| `tests/unit/api/test.test.ts` | `GET /api/test` | DB query with addresses, null user, DB exception |
| `tests/unit/api/health.test.ts` | `GET /api/health` | DB+Redis healthy, DB down (503), Redis down (503), both down (503) |

### ✅ Services (2 test files)
| Test File | Service | What's Covered |
|-----------|---------|---|---|
| `tests/unit/services/orderService.test.ts` | `OrderService` + `calculateDeliveryCharge` | 10+ delivery-charge cases (incl. XSS/SQL-injection strings, unicode, null/undefined), `validateCartItems` (success, empty cart, no cart, insufficient stock, undefined items, no images), `createOrder` (success, country, stock decrement, cart clear, PENDING status, total calc, order-item structure, isReturnable=false for polish, validation fail, default India country), `updateOrderStatus` (SHIPPED/CONFIRMED/CANCELLED, item statuses, return-status exclusion), `updateReturnStatus` (approved, not-found, stock restore, declined, all-returned, partial-returned, single-item), integration edge cases |
| `tests/unit/services/productService.test.ts` | `ProductService` | `buildWhereClause` (category, search, price range, color, ocassion, empty, special chars, unicode, zero prices, ObjectId), `getProducts` (paginated, custom pagination, sorting, filters, review stats), `getProductById` coverage, stock conflict handling |

### ✅ Validators (5 test files)
| Test File | Schemas | What's Covered |
|-----------|---------|---|---|
| `tests/unit/validators.test.ts` | `LoginSchema`, `SignUpSchema`, `ForgotPasswordSchema`, `SendOtpSchema`, `VerifyOtpSchema`, `ResetPasswordSchema` | Happy path, missing/empty fields, invalid email, password complexity (upper/lower/digit/special/length), phone validation, optional address fields, OTP trimming |
| `tests/unit/product-category-validators.test.ts` | `ProductSchema`, `UpdateProductSchema`, `DeleteProductSchema`, `CategorySchema`, `UpdateCategorySchema`, `DeleteCategorySchema` | Name/image/price/stock/categoryId validation, coercion of string→number, negative/zero rejection, empty image array, update/delete partials |
| `tests/unit/cart-checkout-validators.test.ts` | `CartAddSchema`, `CartUpdateSchema`, `CartDeleteSchema`, `CheckoutSchema`, `IdParamSchema`, `ProductQuerySchema`, `PaginationSchema` | Valid/invalid cart items, withPolish default, zero/negative qty/price, missing fields, string coercion, checkout address fields, pagination defaults + null/empty handling, limit cap |
| `tests/unit/edge-cases.test.ts` | `ReturnRequestSchema`, `CartAddSchema`, `CartUpdateSchema`, `CheckoutSchema`, `ProductSchema`, `SignUpSchema`, `IdParamSchema`, `ProductQuerySchema` | XSS strings accepted (sanitized at render), injection strings, boundary numbers (NaN/Infinity), boundary strings (empty/whitespace/10K chars), boolean coercion, type-coercion edge cases, whitespace-only inputs, empty images |
| `tests/unit/more-edge-cases.test.ts` | `CartAddSchema`, `CartUpdateSchema`, `CheckoutSchema`, `ProductSchema`, `SignUpSchema`, `IdParamSchema`, `ReturnRequestSchema`, `ProductQuerySchema` | Mass-parameterised boundary testing across all number/string/boolean extremes for price, stock, quantity, name, email, password — all test cases computed dynamically in loops |

### ✅ Error Classes (2 test files)
| Test File | What's Covered |
|-----------|---|
| `tests/unit/errors.test.ts` | All 5 legacy error classes: `AppError`, `ValidationError`, `AuthError`, `PaymentError`, `NotFoundError`, `DatabaseError` — message, statusCode, isOperational, instanceof, stack trace |
| `tests/unit/errors-extended.test.ts` | All 9 error classes (adds `ForbiddenError`, `ConflictError`, `RateLimitError`, `ExternalServiceError`), `handleApiError` with Prisma P2002/P2025/P2034/generics, circular-reference protection, sensitive-data sanitisation, prototype-pollution safety |

### ✅ Utility / Infrastructure Modules (2 test files)
| Test File | Module | What's Covered |
|-----------|--------|---|---|
| `tests/unit/rate-limit.test.ts` | `rateLimiter()` | Under-limit pass, exact-limit pass, over-limit rejection, per-token independence, interval-based reset |
| `tests/unit/cache.test.ts` | In-memory `cache` fallback | Get null, set/get strings, arrays, complex objects, delete, pattern-clear (TTL expiry), TTL 0 (immediate expiry), key overwrite |

### ✅ React Components (6 test files)
| Test File | Component | What's Covered |
|-----------|-----------|---|---|
| `tests/unit/components/ProductCard.test.tsx` | `ProductCard` | Standard render (name/category/price), 10 edge-case products (zero price, huge price, very long name, empty name, undefined category, empty images, multi-image, negative price, XSS name, long repeated name), priority prop |
| `tests/unit/components/ProductSkeleton.test.tsx` | `ProductSkeleton` | Render + grid of 1/2/5/10 skeletons |
| `tests/unit/components/CartItems.test.tsx` | `CartItems` | Render with polish badge, +/- button clicks, disabled state when loading, remove button |
| `tests/unit/components/CartOrderSummary.test.tsx` | `CartOrderSummary` | Steps 1/2/3 rendering, Proceed-to-Checkout, Continue, Place Order, disabled on placing |
| `tests/unit/components/CartPayment.test.tsx` | `CartPayment` | Saved address mode, new address mode, Edit Address button → setStep |
| `tests/unit/components/CartShipping.test.tsx` | `CartShipping` | Saved addresses render, switch to new-address mode, new-address form fields visible |

### ✅ E2E / Playwright (2 spec files)
| Spec File | What's Covered |
|-----------|---|
| `tests/e2e/homepage.spec.ts` | Page loads with correct title/brand, critical a11y violations check, navigate to All Sarees → `/products`, heading visible |
| `tests/e2e/wishlist.spec.ts` | Navbar wishlist link redirects unauthenticated → `/login?callbackUrl=/wishlist`, wishlist add button on product card redirects to login (graceful skip when DB empty) |

---

## 4. UNIT TEST COVERAGE — What is NOT Tested (Gaps)

### ❌ Untested API Routes (23 routes have 0 unit tests)
| Endpoint | HTTP | Why It Matters |
|---|---|---|
| `POST /api/(auth)/send-otp` | POST | OTP generation + email dispatch is fully untested |
| `POST /api/(auth)/verify` | POST | Session verification is fully untested |
| `POST /api/(auth)/forgot-password` | POST | Password-reset flow is fully untested |
| `POST /api/(auth)/reset-password` | POST | Password-changed logic depends on token validation — untested |
| `DELETE /api/(auth)/signup` | *(if exists)* or other auth variants |  |
| `GET/POST /api/cart` | GET, POST, PUT, DELETE | Fully written, 4 methods, stock/cartId deduplication logic is untested at unit level |
| `POST /api/checkout` | POST | Calls `OrderService.createOrder` — only tested _indirectly_ via `orderService.test.ts` |
| `GET/POST /api/wishlist` | GET, POST, DELETE | Wishlist toggle + DB writes — 0 unit tests |
| `GET /api/categories` | GET | Public listing done via Prisma only — 0 tests |
| `GET /api/categories/[id]` | GET | ID-validated lookup — 0 tests |
| `GET/POST /api/reviews` | GET, POST | Filtering by productId/sort/limit + creation — 0 tests |
| `GET /api/products/[id]` | GET | Public product detail — service method is untested at the route level |
| `GET /api/products/stats` | GET | Product stats endpoint — 0 tests |
| `GET /api/search` | GET | Search service integration — 0 tests |
| `GET/POST/PUT /api/me` | GET, POST, PUT | Profile + address CRUD for logged-in user — 0 tests |
| `GET/POST/PUT/DELETE /api/users/[id]` | all | User admin operations — 0 tests |
| `GET/POST /api/users` | all | Paginated user listing | 
| `GET /api/analytics/sales` | GET | Sales chart data aggregation — 0 tests |
| `GET /api/analytics/track` | GET | Page-view / event tracking — 0 tests |
| `GET /api/auth/[...nextauth]` | GET | NextAuth session/callback provisioning — 0 tests |
| `GET /api/newsletter` | GET | Newsletter public info — 0 tests |
| `GET/POST /api/admin/categories` | all | Admin category CRUD — 0 tests |
| `GET/POST /api/admin/products` | all | Admin product CRUD — 0 tests |
| `GET/POST /api/admin/order` | all | Order management — 0 tests |
| `GET/POST /api/admin/stats` | all | Admin dashboard stats — 0 tests |
| `GET/POST /api/admin/analytics` | all | Admin analytics — 0 tests |
| `POST /api/admin/upload` | POST | Image/file upload — 0 tests |
| `GET/POST /api/admin/email-logs` | all | Email log listing — 0 tests |
| `GET/POST /api/admin/newsletter` | all | Newsletter sends — 0 tests |
| `GET/POST /api/admin/reviews` | all | Bulk review moderation — 0 tests |
| `GET/POST /api/admin/ai-insights` | all | AI insights generation — 0 tests |
| `GET/POST /api/emails/queue-status` | all | BullMQ queue status — 0 tests |
| `POST /api/emails/send-newsletter` | POST | Newsletter email dispatch — 0 tests |
| `POST /api/emails/send-order-confirmation` | POST | Order-confirmation email — 0 tests |
| `POST /api/emails/send-welcome` | POST | Welcome-email delivery — 0 tests |
| `GET/POST/PUT/DELETE /api/orders/[id]` | GET, etc. | User order lookup — 0 tests |
| `POST /api/orders/[id]/cancel` | POST | Cancel + restock logic — 0 tests |
| `POST /api/orders/[id]/return` | POST | Return + stock restore + order status merge — 0 tests |
| `GET /api/site-settings` | GET | Public site-settings lookup — 0 tests |
| `GET/PUT /api/products/[id]/stats` | all | Product stats — 0 tests |

### ❌ Untested Library Modules
| Module | Location | Reason It Matters |
|---|---|---|
| **Auth logic** | `lib/auth.ts` | Password hashing, session token generation, role checking — 0 tests |
| **Session decoding** | `app/lib/getUserFromToken.ts` | JWT decode + expiry + role middleware — 0 tests |
| **Cart actions** | `app/lib/cartActions.ts` | Cart state persistence + cookie read — 0 tests |
| **Cookie helpers** | `app/lib/cartCookie.ts` | Cookie get/set helpers — 0 tests |
| **Email queue** | `lib/email/emailQueue.ts` | BullMQ queue add/retry/error handling — 0 tests |
| **Email service** | `lib/email/emailService.ts` | Nodemailer send + template rendering — 0 tests |
| **Email templates** | `lib/email/templates/*` | HTML email templates — 0 tests |
| **Search service** | `lib/searchService.ts` | Search logic — 0 tests |
| **Analytics service** | `lib/analyticsService.ts` | Analytics aggregation — 0 tests |
| **Image service** | `lib/imageService.ts` | Image optimisation / upload — 0 tests |
| **Redis wrapper** | `lib/redis.ts` + `lib/rate-limit-redis.ts` | Redis client factory + Redis-backed rate limiter — 0 tests (rate-limiting tests only cover the in-memory fallback) |
| **Rate limit wrapper** | `lib/rateLimitWrapper.ts` | Async HOF used in API routes — 0 tests |
| **Mail module** | `app/lib/mail.ts` | Nodemailer transport — 0 tests |

### ❌ Untested / Minimally-Tested Page Components
| Page/Section | Location | Gap |
|---|---|---|
| Login page | `app/(auth)/login/page.tsx` | 0 unit tests |
| Signup page | `app/(auth)/signup/page.tsx` | 0 unit tests |
| Forgot-password page | `app/(auth)/forgot-password/page.tsx` | 0 unit tests |
| Reset-password page | `app/(auth)/reset-password/page.tsx` | 0 unit tests |
| Cart page | `app/(pages)/cart/page.tsx` | 0 unit tests |
| Product detail page | `app/(pages)/product/[id]/page.tsx` + `product-detail-client.tsx` | 0 unit tests |
| Products list page | `app/(pages)/products/page.tsx` | 0 unit tests |
| Checkout page | `app/(pages)/checkout/page.tsx` | only `CartShipping`, `CartPayment`, `CartOrderSummary` sub-components tested individually |
| Wishlist page | `app/(pages)/wishlist/page.tsx` | 0 unit tests |
| Profile page | `app/(pages)/profile/page.tsx` | 0 unit tests |
| Orders list page | `app/(pages)/orders/page.tsx` | 0 unit tests |
| Orders detail page | `app/(pages)/orders/[id]/page.tsx` | 0 unit tests |
| Invoice page | `app/(pages)/orders/[id]/invoice/page.tsx` | 0 unit tests |
| Return item page | `app/(pages)/orders/[id]/return/[itemId]/page.tsx` | 0 unit tests |
| Hero section | `app/components/home/hero-section.tsx` | 0 unit tests |
| Reviews section | `app/components/home/reviews-section.tsx` | 0 unit tests |
| Category scroll | `app/components/home/category-scroll.tsx` | 0 unit tests |
| Offers section | `app/components/home/offers-section.tsx` | 0 unit tests |
| Trust strip | `app/components/home/trust-strip.tsx` | 0 unit tests |
| Auth provider | `app/contexts/auth-context.tsx` | 0 unit tests |
| Cart context | `app/contexts/cart-context.tsx` | 0 unit tests |
| Wishlist context | `app/contexts/wishlist-context.tsx` | 0 unit tests |
| Product info/accordion/gallery/related | `app/components/product/*` | 0 unit tests |
| Admin dashboard page | `app/admin/page.tsx` | 0 unit tests |
| Admin layout | `app/admin/layout.tsx` | 0 unit tests |
| Analytics provider | `app/components/analytics-provider.tsx` | 0 unit tests |
| Navbar & footer | `app/components/*` | 0 unit tests |
| Cookie consent | `app/components/cookie-consent.tsx` | 0 unit tests |

### ❌ Integration / Cross-Layer Gaps
| Gap | Detail |
|---|---|
| **Auth → API → DB** | No test hits a real `getUserFromToken` + Prisma + API route end-to-end |
| **Cart add + stock check** | Not tested as a session — only via unit tests of individual classes with mocks |
| **Order creation → email queue** | `orderService.test.ts` tests the service, but the route (`POST /api/checkout`) has no unit tests |
| **Image upload → service → DB** | Zero coverage of the upload pipeline |
| **Newsletter send → email queue** | No test of `POST /api/emails/send-newsletter` or `admin/newsletter` |
| **Admin CRUD → service → DB** | All admin API routes are untested |
| **Return flow end-to-end** | Return route writes a file to disk — not covered by unit tests; service method tested in isolation only |
| **Session expiry in JWT decode** | `getUserFromToken` tests absent — buggy expiry logic would go undetected |

### ⚠️ Superficial Coverage (Has a test but it's thin)
| Test | Issue |
|---|---|
| `direct-import.test.ts`, `routeImportTest.test.ts` | Assert handlers are callable — do **not** mock any dependency or assert response shape. These guarantee nothing about business logic. |
| `products.test.ts` — "error handling" test | Verifies `getProducts` *rejects* on mock error but asserts nothing about the HTTP status or response body of the NextResponse |
| `signup.test.ts` | Missing: email-queue call (`emailQueue.add`) verification, rate-limit middleware coverage, SendOtpSchema usage |

---

## 5. E2E Coverage — Summary

| Area | Coverage |
|---|---|
| Homepage (load + brand) | ✅ |
| Homepage critical accessibility | ✅ (axe-core) |
| Homepage → Products navigation | ✅ |
| Wishlist → login redirect (unauthenticated) | ✅ |
| Wishlist add button → login redirect | ✅ (graceful skip on empty DB) |
| Login / register flow | ❌ |
| Cart → Checkout flow | ❌ |
| Place order → order confirmation | ❌ |
| Product detail | ❌ |
| Admin dashboard | ❌ |
| Profile / address management | ❌ |
| Search / filter / sort | ❌ |

---

## 6. Summary Table

| Category | Untested Routes/Methods | Procedural (mocked) | Structural / Integration | E2E |
|---|---|---|---|---|
| Auth (6 routes) | 4 (send-otp, verify, forgot-pw, reset-pw) | 1 (signup) | 0 | 0 |
| Cart (CRUD) | 4 | 0 | 0 | 0 |
| Checkout (1 route) | 1 | 0 (indirect via service) | 0 | 0 |
| Checkout sub-components | 0 | 3 (CartShipping/Payment/OrderSummary) | 0 | 0 |
| Wishlist (CRUD) | 2 | 0 | 0 | 1 (redirect only) |
| Products (list + detail + stats) | 3 | 1 (list) | 0 | 0 |
| Categories (list + detail) | 2 | 0 | 0 | 0 |
| Reviews | 2 | 0 | 0 | 0 |
| Order (list + detail + cancel + return) | 4 | 0 | 0 | 0 |
| User / Profile | 3 | 0 | 0 | 0 |
| Search | 1 | 0 | 0 | 0 |
| Emails (queue + send-*, send-*) | 4 | 0 | 0 | 0 |
| Analytics (sales + track) | 2 | 0 | 0 | 0 |
| Admin (10 route groups) | 10 | 0 | 0 | 0 |
| Services | 0 | 2 (order, product) | 0 | 0 |
| Validators | 0 | 5 (full) | 0 | 0 |
| Error classes | 0 | 2 (full) | 0 | 0 |
| Cache / Rate-limit | 0 | 2 | 0 | 0 |
| Utility / lib modules | 10+ files | 0 | 0 | 0 |

---

## 7. Overall Risk Assessment

| Risk | Severity | Reason |
|---|---|---|
| Admin panel has **zero test coverage** | 🔴 High | 10 new admin API routes for critical ops (categories/products/orders/stats/analytics/upload/email-logs/newsletter/reviews/ai-insights) have no tests |
| Auth flows partially tested | 🟡 Medium | Signup has good coverage; send-otp, verify, forgot-pw, reset-pw are all untested |
| `getUserFromToken` untested | 🔴 High | JWT identity gate for 20+ protected routes has 0 unit tests |
| BullMQ email pipeline untested | 🟡 Medium | Email queue & send-* routes have no tests — failed emails go undetected |
| Cart/checkout integration only indirectly tested | 🟡 Medium | No integration test of PUT→recalc→checkout flow end-to-end |
| E2E coverage very thin | 🟡 Medium | Only 5 Playwright assertions covering 2 pages |
| Utilities (redis, auth helpers, mail) untested | 🔴 High | Auth token generation and mail transport are security-critical |
| Validator coverage is excellent | ✅ Low risk | 5 dedicated files covering all Zod schemas with extensive edge cases |
| Service-layer coverage is strong | ✅ Low risk | `orderService` and `productService` have thorough unit tests |
| Error class coverage is strong | ✅ Low risk | 9 error types + `handleApiError` fully tested |

---

## 8. Recommended Priority Order (Not Implemented — Observation Only)

1. **Write unit tests for `getUserFromToken` + `auth.ts`** — these are the security gates for all protected routes  
2. **Write unit tests for `emailQueue` and `emailService`** — critical communication pipeline  
3. **Write API route tests for the 4 missing auth flows** — OTP, verify, forgot-pw, reset-pw  
4. **Write API route tests for `POST /api/cart` (POST, PUT, DELETE)** — stock management is complex and already bug-prone  
5. **Write API route tests for `POST /api/checkout`** — main business-flow exit point  
6. **Write API route tests for `POST/PUT/DELETE /api/orders/[id]/cancel`** + `return` — stateful DB transaction  
7. **Write API route tests for all 10 admin routes** — admin panel is the least-tested area  
8. **Add E2E tests: Login → Add to Cart → Checkout → Place Order** — the full purchase flow is untested in E2E  
9. **Add unit tests for `redis.ts`, `rate-limit-redis.ts`, `rateLimitWrapper.ts`** — infrastructure reliability  
10. **Add page-level component tests for Login, Signup, Product Detail, Cart page** — currently driven only by E2E wait cycles
